import { GlassInput } from "@/components/ui/GlassInput";
import { Bell, Search } from "lucide-react";

export function TopBar() {
    return (
        <header className="h-6 flex items-center justify-between px-8 py-4 mb-2">
            {/* Empty TopBar as requested */}
        </header>
    );
}
